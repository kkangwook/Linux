1. cd이동: 상위, 현재, 내홈 두가지방법, 바로전에 있던 디렉토리, 최상위
 
2. 디렉토리 x->y->z 만들기 -> z 안에 파일 text.txt만들기 -> x로 카피하기 ->y로 이동시키기 -> 파일지우기 ->디렉토리지우기  

3. 그림파일 무슨 확장자인지

4-249. 로그관련 디렉토리에서 .log들 파일 찾기

5. text.txt에 아무거나 넣기 3가지 방법(echo, cat, vi) -> 페이지단위로 보기

6-546. 로그인한 사용자 계정명이 있는 행만 출력
6-549. 사용자 계정으로 실행한 프로세스를 모두 출력

7-650. date날짜 전부 나타내는법: date +%y-%m-%d-%H-%M-%S

8-300. sed로 밑의 sample파일을 만들고 옆처럼 고쳐서 전부 출력
bob	M		bob	male
jane	F	->	jane	female
tony	m		tony	male
soo	f		soo	female
			woo

9-319. awk로 밑의 df 파일 만들고 sum=0으로 지정한뒤 2열이 25이상인 행에 대해 4열값을 출력하고 이 값을 전부 더해 마지막에 "sum : "식으로 출력
a	10	11	12
b	20	21	22
c	30	31	32
d	40	41	42

10-372~415. 백그라운드로 more로 서비스 보기->pid찾기->죽이기

11-421. tcsh쉘 다운받고 -> 내가가진 쉘리스트보고->변경->확인 -> 원상태로 돌리기

12-437. fname=woohyun, score=90 변수만들고, 변수 각각 보고, 목록으로보고 제거하기

13-451주요환경변수들, 환경변수위치보기 -> 환경변수등록 코드는?(그냥 변수/스크립트) -> 영구저장 위해서는?
										export fname=woohyun, export PATH="$PATH:$HOME/myscripts"   -> vi ~/.bashrc에 export붙여넣기 -> source ~/.bashrc 

14-507. rrm이라는 rm -r 기능을 가진 alias만들고 alias목록보고 지우기

15-648. bashrc파일 어디있는지 찾기(에러출력 안되게 하면서)

16-541. /bin 디렉터리에 있는 모든 파일을 디스크 사용량에 따라 오름차순으로 정렬해 크기가 큰 상위 7개 파일만 출력 

17-578. 위치매개변수 쉘 만들고 cho woo hyun각각 파일옆에 입력받아 제목,각변수, 개수, 리스트로 출력되게 +실행

18-619. 섭씨온도 입력받아 화씨온도 **소수점 2째자리까지** 출력하는 스크립트짜기
	°F = (°C × 9/5) + 32

19-698. 파일이름입력받아 존재유무로 exists or nope 출력하는 스크립트 짜기 두가지 방법(test,  [ ~ ] )
				test는 그냥, [ ~ ]는 not 버전으로!!!

20-743. 숫자입력받아 100보다 크거나 2의배수이면 hi, 100보다작고 10보다크면 good, 그외는 bye출력
	이때 위에 cat<<END ~ END로 위의 조건 각 줄력별로 입력 + 그 외시 exit 1 -> $?로 1나오는지 확인

21-798. while문: number: 1 ~ number: 5까지 출력하고 모든 값 더한값 마지막에 출력

22-835. while+if로 1부터 10까지 수를 출력하고, 홀수인지 짝수인지 구분

23-851. for문 + seq로 1~10출력후 모든수 더한값 출력

24-882. for문으로, 파일옆에 매개변수들을 받아 순차적으로 더해진값을 출력하고 최종 합도 구하는 쉘 프로그램을 작성


------------------------------------------------------파트2-------------------------------------------------------------
1-1-21. 사용자 홈 디렉터리에서 archive란 이름의 디렉터리를 생성하고 test01부터 test100까지 100개의 빈 파일을 생성 
그리고 archive 디렉터리를 모두 묶어 archive.tar 파일을 생성 -> 생성한 archive.tar 파일의 크기는 몇 바이트입니까?

1-2-31.  사용자 홈 디렉터리에서 backup이란 이름의 디렉터리를 생성하고 위에서 만든 archive.tar 파일을 복사 ->  archive.tar 파일을 해제

2-51. /usr/lib 경로에서 5 메가바이트 ~ 10 메가바이트 사이의 파일을 찾아 사용자의 홈 디렉터리 archive로 복사

2-57. 위에서 복사한 파일을 gzip, bzip2, xz, zip 명령어를 통해 압축하고, 크기를 비교 -> archive밑에 gz라는 이름으로 해제하기
		이때 내가 원하는 위치로 해제하기 : gzip -dc [압축파일.gz] > [저장할경로/파일명]  #c옵션 사용
######################################################################################
3. mount 5가지 과정은 무엇인가 -> 실습
디스크는 /dev 안에 존재

디스크(데이터 저장하는 물리적 저장장치) 사용을 위한 작업 flow
1. 디스크 준비 및 파티션 생성(준비하고 쓸 부분 정하기)
   → 새 디스크를 추가하고 fdisk로 사용할 파티션을 생성
	$ 우선 ppt에 vmware에서 하드디스크1GB추가
	$ ls /dev/sd* 하면 새로생긴거 있음 (숫자 안붙은게 논리장치 아직 안생긴 새로운 디스크)
	$ sudo fdisk /dev/sdb  -> fdisk로 sdb를 논리적으로 분할
	$ n으로 새로운 파티션 분할
	$ p: primary -> 기본
	$ partition number1로 -> sdb1이됨
	$ first sector는 이미 정해짐 -2048
	$ last sector도 그냥 enter하면 기본값으로 설정
	$ p로 확인
	$ w로 파티션분할 저장
	논리적으로 디스크 파티션 설정하면 논리적 장치에 1 붙게됨 -> 얘를 포맷
		sdb라는 물리적장치에 대해 설정하면 sdb1, sdb2, .... 이렇게 파티션으로 논리적으로 나눠서 사용
	$ ls /dev/sd*하면 sdb1생성된것을 볼 수 있음

------------------------------------------------------------------------------------------------------------------------
2. 디스크 포맷(그 저장공간에 파일을 저장할 수 있도록 파일시스템을 만드는 것)
   → mkfs(make file system) 명령어로 파티션에 파일시스템(ext4, xfs 등)을 생성
	$ sudo mkfs.ext4 /dev/sdb1  #파일 시스템을 만드는 mkfs를 통해 /dev/sdb1 논리적 파티션을 ext4 파일 시스템 형식으로 변환 (파티션 포맷)

------------------------------------------------------------------------------------------------------------------------
3. 장치 연결(마운트)
   → mount 명령어로 파티션을 특정 디렉터리에 연결하여 사용할 수 있게 함
	sudo mount <옵션> <장치명> <디렉토리명> -> 어떤 장치를 어느 디렉토리에 연결하겠다
	$ mkdir /mnt/mydata    #디스크를 연결할 디렉토리를 생성 -> 주로 /mnt에 디렉토리 생성
	# touch /mnt/mydata/test1
	$ sudo mount /dev/sdb1 ./mydata	# 디스크와 디렉토리를 연결
	# ls ./mydata 하면 test1 잠시사라지고 lost+found자동생성된것을 볼 수 있음
	# touch ./mydata/test2
	mount | tail -1 로 마운트 무엇이 됐는지 확인
------------------------------------------------------------------------------------------------------------------------
4. 부팅 시 자동 마운트 설정 # 얘는 안해도 됨
   → /etc/fstab에 등록하여 재부팅 후에도 자동으로 마운트되도록 설정

------------------------------------------------------------------------------------------------------------------------
5. 마운트해제 (umount)
	사용중일경우 target is busy뜸
	umount하면 mount하면서 작업했던것들 중 디렉토리는 남아있지만 file은 안남아있음
	$ sudo umount /dev/sdb1    # 장치 제거
	#ls ./mydata하면 test2사라지고 test1이 다시 돌아옴

6. 파티션 삭제(다음번 실습위해)
sudo fdisk /dev/sdb -> d(파티션 삭제) -> w(저장)

######################################################################################

4-134. a@b 에서 a와 b는 각각 뭐인지 -> 사용자보는법(2가지) -> 그룹 보는법, 나는 어느 그룹에 속해있나

5-144. user1 계정과 user2 계정을 홈 디렉토리와 함께 생성한 뒤, 추가 정보들 보기
두 계정을 삭제하는데, 단 user2 계정을 삭제할 때는 -r 옵션을 사용한 뒤 /home 디렉터리의 차이점을 확인하시오. 

6. dev 그룹과 tester 그룹을 생성 후 dev에 user1, user2/ tester에 user3, user4추가 -> 계정,그룹 삭제


######################################실습#######################################
7. 새로운 계정에 superdo 권한 부여하기 -> 1. sudo group에 포함되기
	2. sudo visudo들어가서 user ALL=(ALL) ALL 행 추가
문제: /etc/sudoers 파일을 수정해 관리자(manager) 계정을 만들고 이 계정에 useradd와
userdel 시스템 명령어를 sudo 권한을 빌려 사용할 수 있도록 수정하시오.
관리자(manager) 계정으로 로그인 후 user5 계정을 생성하시오

일단 manager만듬
sudo chmod 660 /etc/sudoers로 편집가능하게  <- 이건 자제해야함!!!!!!!!!
vi 편집기로

-> sudo visudo로 편집 !!!!!!!!!!!!
manager ALL=(ALL) ALL
-> manager로 로그인 by su - manger
-> 이제부터 sudo됨

manager ALL= /sbin/useradd, /sbin/userdel   => manager 사용자는 sudo를 이용해서 useradd, userdel 명령을 실행할 수 있게됨
이후 추가하고싶은 명령어 ex) sudo apt ~하고 싶으면 whereis apt하면 전체 디렉토리나옴 -> ALL= /usr/bin/apt  이런식으로 하면 됨

######################################################################################

8. chmod 각 숫자 의미

9-179. 유저 바꾸는 명령어, 파일에 대한 소유자명과 그룹명 바꾸는법

###########################아파치 사용법#################################
10. systemctl: apache(웹서버 서비스)와 같은 서비스들 시작/중지/상태 확인
	systemctl <명령> <서비스명>
	$ systemctl list-units			#서비스들 출력
	$ sudo systemctl status apache2	#서비스상태확인
	$ sudo systemctl start apache2	#서비스 시작
	$ sudo systemctl stop apache2	# 서비스 중지
	$ sudo systemctl enable apache2	# 재부팅 후 자동으로 서비스 실행되도록
	$ sudo systemctl disable apache2	# enable된 서비스 해제


sudo apt install apache2 하고 firefox에서 localhost(나자신)들어가면 접속 뜸 -> ip addr 하면 inet이 ip주소
	locathost에 보면 디렉토리 주소있음. 거기서 html을 sudo vi ~~~/~html해서 http작성하면 홈페이지 바뀜
• 방화벽으로 http 프로토콜을 거부 / 허용 했을 때 웹 페이지 접속이
가능한지 확인하시오.
-> 설정에서 NAT(내부끼리만) -> bridged(외부와접속가능)로 바꿔야함!!!!! ->그리고 apache2켜져있어야함-> 그리고 ip addr로 나온 192.xxxxx를 크롬에서 접속 가능!!!!!
여기서 이제 sudo ufw로 http를 allow하거나 deny하면 됨

#######################################################################

11. 계정 비밀번호가 저장된곳, 서비스보면서 포트번호도 같이 보기, DNS 서버 주소 저장 장소, 호스트 정보, 자기 ip보기, 웹 서버가 웹사이트 파일을 저장하는 디렉터리 위치는?        -> /etc/shadow, /etc/services, /etc/resolv.conf, hostnamectl status, 
																																												ip addr, /var/www/html


#############################로그 기록되는 파일 만들기###########################
#문제
구성파일(/etc/rsyslog.conf)을 수정해 시스템 로그를 /var/log/infolog 파일에 저장되도록 구성하시오. (/var/log/infolog 파일 미리 만들어 놓으면 안됨!!!!!!!-> 만들었으면 제거해야함)

sudo vi /etc/rsyslog.conf 하고 맨 밑에줄로 가서 *.info /var/log/infolog 하고
sudo systemctl restart rsyslog 로 재시작, sudo tail /var/log/infolog로 있는지 확인
ls로 /var/log/infolog파일 생성됐는지 확인 -> 파일 생성
이제 su - user1으로 로그인 했다가 exit으로 로그아웃 한다음 cat /var/log/infolog하면 user1으로 로그인 했다가 로그아웃한 로그가 뜸
=> 즉 infolog라는 파일에 로그 저장되게 한 것!!!!!
tail -f infolog하고 다른 터미널에서 작동하면 tail출력값이 계속 로그 업로드
원하는 로그만 보고싶으면 cat /var/log/infolog | grep user1
#######################################################################


################################NFS실습#######################################
NFS: **인터넷을 통해** 다른 리눅스 컴퓨터에 있는 디렉터리를 내 컴퓨터의 디렉터리처럼 사용하는 기능
	-> 리눅스의 하드디스크를 네트워크에 접속시키고 우리는 이 네트워크에 원격으로 접속해서 관리하는법!!!!!

-----------두 서버가 필요!!!! + 서로가 서로의 ip를 사용!!!!!!!-----------
1(서버VM). NFS 서버를 위한 패키지 설치(디스크 할당된 디렉토리 공유하는쪽)
	-> sudo apt install nfs-kernel-server

2(서버VM). 디렉토리 만들고 모든권한주기 -> 이 디렉토리를 공유 -> 얘는 뭐 디스크같은거 마운트 한게 아니라 그냥 순수 서버의 저장공간
	mkdir ~/share    -> chmod 777 share

3(서버VM).  /etc/exports: 어떤 서버의 디렉토리를 네트워크에 올릴지, 또 어떤 클라이언트가 접속할지 클라이언트의 ip주소 마지막줄에 추가 -> 우리가 여기 연결해서 파일 관리한다
	: [공유디렉터리] [클라이언트 ip주소-ip addr값] (옵션)
	ex) /home/woohyun/share 192.168.1.70(rw,sync) 를 /etc/exports 마지막 줄에

4(서버VM).NFS 서비스가 변경된 /etc/exports 반영해서 nfs서비스 시작
- sudo exportfs -a     → /etc/exports에 작성한 모든 공유 설정을 적용(export)
- sudo systemctl restart nfs-kernel-server     →  우분투, 데비안 계열의 NFS 서버 서비스를 재시작
#- sudo systemctl restart nfs-server # 로키, RHEL, CentOS 계열
- sudo exportfs -v 	→ 현재 적용된 NFS 공유 설정을 상세하게 확인 -> 추가된 자동 설정들

5(서버VM).방화벽 허용 
	 - sudo ufw allow nfs

---------------------------이제 다른 클라이언트 서버로-------------------------------------------------------

6(클라이언트VM). NFS 클라이언트 (공유된 디렉토리 가져다 쓰는쪽) 패키지 설치
- sudo apt install nfs-common # 우분투, 데비안 계열 설치 필요
# sudo apt install nfs-utils # 로키, RHEL, CentOS 계열

7(클라이언트VM). 클라이언트가 서버의 공유 디렉터리를 클라이언트 자신의 /mnt/share에 마운트 -> 서버 ip입력
sudo mkdir -p /mnt/share
sudo mount 192.168.1.37:/home/woohyun/share /mnt/share
	-> mount가 "이건 NFS 원격 경로구나”라고 판단해서 NFS용 마운트 도구를 이용해 연결하는 구조

8(클라이언트VM). 마운트 확인: mount | grep nfs   -> clientaddr 70, addr=37

9(클라이언트VM). /etc/fstab 파일 수정 -> 부팅할 때 자동으로 마운트되도록 등록하는 과정
	-> 마지막줄에 : [NFS 서버의 IP 주소와 공유 디렉터리] [마운트 지점] [파일시스템 종류] [옵션] [dump] [pass]
		ex) 192.168.1.37:/home/woohyun/share /mnt/share nfs defaults 0 0

10: 서버의 ~/share와 클라이언트의 /mnt/share가 같은 파일을 보여주게 됨
	-> 각각 cat으로 파일 생성하면 동시에 생성
#######################################################################



================3티어 구축=================
1. 웹서버 패키지 준비
	sudo apt -y install apache2

2. 동적 웹 스크립트(PHP) 패키지 설치
	sudo apt -y install php
	php -v로 확인

3. /var/www/html에 info.php파일 생성
	sudo vi /var/www/html/info.php
		->밑에걸 적음 
		<?php				#PHP 코드라는 표시
			phpinfo();			#현재 서버의 PHP 버전, 설정, 모듈 등의 정보를 웹페이지로 출력
		?>       

4. mysql설치
	sudo apt -y install mysql-server	#mysql설치
	sudo apt -y install php-mysql	#php-mysql연동을 위한 패키지도 설치
	sudo systemctl start mysql		# mysql 서비스 시작

5. 워드프레스라는 오픈소스 웹 콘텐츠 관리 시스템 사용(우리는 개발자가 아니라 만들어진거 사용)
	wget https://wordpress.org/latest.tar.gz   # wget으로 워드프레스 압축파일 다운받아오기
	tar -xzf latest.tar.gz				# 받아온 파일 압축해제 및 풀기 -> wordpress 디렉토리가 생성됨
	sudo cp -r wordpress /var/www/html/	# /var/www/html에 이 wordpress파일 옮김 -> Apache가 웹으로 제공하는 /var/www/html/ 아래로 복사
	sudo chown -R www-data:www-data /var/www/html/wordpress  # 소유자,그룹 www-data로 변경 (아파치가 사용하는 소유자와 그룹)
	sudo chmod -R 755 /var/www/html/wordpress				# 권한도 변경

6. mysql 실행 및 프로프트 작성(wordpress에 쓰일 DB)
	sudo mysql -u root -p		# sql입력창 열림

CREATE DATABASE wordpress;			-> var/lib/mysql/wordpress로 저장됨
CREATE USER 'admin' IDENTIFIED BY '1234';
GRANT ALL PRIVILEGES ON wordpress.* TO 'admin';
FLUSH PRIVILEGES;
EXIT;

7. 워드프레스 설치 마법사  -> 여기서 워드프레스랑 DB 연동됨
	http://IP주소/wordpress로 이동
	-> 언어 설정하고 db이름, 사용자명, 비번 입력 (이 정보들 적으면 알아서 연동해줌)
	->만들 페이지이름,사용자,비번,이메일등입력
	-> 페이지 생성 !!!!!!!
==========================================
